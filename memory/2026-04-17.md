# 2026-04-17

- The root `MEMORY.md` was discovered missing after the session history was lost when the PC went into standby.
- Searched for old memory copies. `Intel/memory.md` exists but only contained a test line, so it was not useful as a real recovery source.
- Recovered Apollo continuity from `projects/apollo/` control docs and from the archived session note `legacy/Intel/workspace/memory/2026-04-16-lightrag-ingestion.md`.
- Rebuilt a fresh root `MEMORY.md` with the durable Apollo context and continuity rules.
- Added a `memory/backups/` backup path and a simple backup script so future `MEMORY.md` changes can be mirrored.
- Updated `AGENTS.md` with instructions to restore `MEMORY.md` from backup if it ever goes missing again.
- Added a local `scripts/memory_backup_daemon.sh` watchdog and started it with a 10-minute interval so `MEMORY.md` is backed up automatically.
- The user added a new workspace rule: before modifying core project/runtime files like `docker-compose.yaml`, `apollo_up.bat`, `apollo_down.bat`, `.env`, and similar files, create dated/versioned backups in `legacy/Backups/`.
- Seeded `legacy/Backups/` with backups of `docker-compose.yaml`, `apollo_up.bat`, `apollo_down.bat`, `.env`, and `AGENTS.md`, and added `scripts/backup_core_project_files.sh` to make that easier to follow consistently.
- Backed up `docker-compose.yaml` and `.env` again as `v2` snapshots before hardening them.
- Hardened `docker-compose.yaml` with restart policies, `init: true`, graceful stop periods, and core service healthchecks, while keeping remote-access port exposure unchanged.
- Moved compose-managed secrets into `.env` and added `POSTGRES_PASSWORD`, `WEBUI_SECRET_KEY`, and the existing `N8N_ENCRYPTION_KEY` there.
- Deliberately did **not** switch `n8n` to Postgres in this pass, to avoid silently stranding the existing SQLite-backed state.
- Updated Apollo control docs to record the hardening decisions and next validation steps.
- Backed up the core project files again as `v3` snapshots before rewriting `apollo_up.bat` and `apollo_down.bat`.
- Rewrote `apollo_up.bat` to detect Docker/Compose, wait for health instead of sleeping blindly, and only pull `nomic-embed-text` if it is missing.
- Rewrote `apollo_down.bat` to do ordered shutdown, verify containers are no longer running, and avoid pretending that `nvidia-smi` itself releases VRAM.
- After restart the full Apollo stack came back healthy (`docker-compose ps` showed all core services healthy), and the `MEMORY.md` backup daemon was restarted successfully.
- Recovered the previously lost session content into `legacy/Recovered-Sessions/2026-04-17-lost-session-recovery.md`, including the exact recovered "ok lets proceed to definig contract then" exchange and the key Apollo decisions recovered from saved session transcripts.
# 2026-04-17

- The root `MEMORY.md` was discovered missing after the session history was lost when the PC went into standby.
- Searched for old memory copies. `Intel/memory.md` exists but only contained a test line, so it was not useful as a real recovery source.
- Recovered Apollo continuity from `projects/apollo/` control docs and from the archived session note `legacy/Intel/workspace/memory/2026-04-16-lightrag-ingestion.md`.
- Rebuilt a fresh root `MEMORY.md` with the durable Apollo context and continuity rules.
- Added a `memory/backups/` backup path and a simple backup script so future `MEMORY.md` changes can be mirrored.
- Updated `AGENTS.md` with instructions to restore `MEMORY.md` from backup if it ever goes missing again.
- Added a local `scripts/memory_backup_daemon.sh` watchdog and started it with a 10-minute interval so `MEMORY.md` is backed up automatically.
- The user added a new workspace rule: before modifying core project/runtime files like `docker-compose.yaml`, `apollo_up.bat`, `apollo_down.bat`, `.env`, and similar files, create dated/versioned backups in `legacy/Backups/`.
- Seeded `legacy/Backups/` with backups of `docker-compose.yaml`, `apollo_up.bat`, `apollo_down.bat`, `.env`, and `AGENTS.md`, and added `scripts/backup_core_project_files.sh` to make that easier to follow consistently.
- Backed up `docker-compose.yaml` and `.env` again as `v2` snapshots before hardening them.
- Hardened `docker-compose.yaml` with restart policies, `init: true`, graceful stop periods, and core service healthchecks, while keeping remote-access port exposure unchanged.
- Moved compose-managed secrets into `.env` and added `POSTGRES_PASSWORD`, `WEBUI_SECRET_KEY`, and the existing `N8N_ENCRYPTION_KEY` there.
- Deliberately did **not** switch `n8n` to Postgres in this pass, to avoid silently stranding the existing SQLite-backed state.
- Updated Apollo control docs to record the hardening decisions and next validation steps.
- Backed up the core project files again as `v3` snapshots before rewriting `apollo_up.bat` and `apollo_down.bat`.
- Rewrote `apollo_up.bat` to detect Docker/Compose, wait for health instead of sleeping blindly, and only pull `nomic-embed-text` if it is missing.
- Rewrote `apollo_down.bat` to do ordered shutdown, verify containers are no longer running, and avoid pretending that `nvidia-smi` itself releases VRAM.
- After restart the full Apollo stack came back healthy (`docker-compose ps` showed all core services healthy), and the `MEMORY.md` backup daemon was restarted successfully.
- Recovered the previously lost session content into `legacy/Recovered-Sessions/2026-04-17-lost-session-recovery.md`, including the exact recovered "ok lets proceed to definig contract then" exchange and the key Apollo decisions recovered from saved session transcripts.
- The next environment-authority priorities were identified as: working Docker/Compose control from the OpenClaw runtime, proper Git integration, persistent startup for maintenance helpers, and eventually an Apollo-specific skill.
- A key architectural conclusion was reached: Apollo should likely use a hybrid dual-plane model, where the normal assistant remains inside the Apollo stack but at least one out-of-band control/recovery path exists outside it.
- That dual-plane architecture question was intentionally pinned for later because the stack is currently functional; the immediate focus returns to Project Apollo itself.
- Recovered the longer Apollo planning thread into `legacy/Recovered-Sessions/2026-04-16-long-apollo-planning-session.md`, restored a live `.jsonl` copy as `Intel/agents/main/sessions/1a218bee-b0ee-4a58-8724-239ef5ecf788.jsonl`, and registered it as a custom OpenClaw session key: `agent:main:apollo-planning-recovered`.
